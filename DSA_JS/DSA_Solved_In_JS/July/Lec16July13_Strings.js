const stringsValue = (strings) => {
    // console.log(Object.values(strings).join(' ')); // Extract values and join
    console.log(strings);
};

const strings = { a: 'A', b: 'B', c: 'C', d: '2', e: '@' }; // Correct object syntax
stringsValue(strings);
